using UnityEngine;
using System.Collections;

public class checkScript : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{



	    //var lightVec = GameObject.Find("Directional Light").transform.forward;
	    //GetComponent<MeshRenderer>().material.SetVector("L",lightVec);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
